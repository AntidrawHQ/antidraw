// Drizzle schema for the antidraw cloud database (Cloudflare D1 / SQLite).
//
// better-auth's tables (user, session, account, verification) land here in the
// next step — generate them with `npx @better-auth/cli generate` — alongside
// any app tables (e.g. publish/sync entitlements). Empty for now so the Worker
// scaffold stays free of auth concerns.
export {};
